<%= form_for @post,  html: { multipart: true } do |f| %>
 <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li class="wrrred"><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field ffc">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field ffc">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field ffc">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions ffc">
    <%= f.submit %>
  </div>


<% end %>
